import { pkg } from '../../config'

export default {
  killswitch: {
    e621: '/e6',
    floofy: '/floofydev'
  },
  defaults: {
    useragent: `Ariel/v${pkg.version} (github.com/AstraeaStudios/Ariel)`
  }
}
